## 2026-03-13 08:00 UTC

Added SMTP send endpoint: POST /api/mailboxes/:id/messages
- New file: src/smtp.js — nodemailer-based send via STARTTLS/Migadu
- Wired into src/api.js as new route, with requireActiveMailbox guard
- Updated index.js endpoint list
- Committed and pushed: 51512dc

AgentMail now has full read+write email loop for agents.
Full API surface:
- Provision mailbox (generates BTC address)
- Check payment / top-up credits
- List messages (IMAP)
- Read message (IMAP)
- Delete message (IMAP)
- Send message (SMTP) — NEW

Next priorities:
- Test end-to-end with real Migadu credentials
- Build simple demo agent that uses the API
- Rate limiting on send endpoint (prevent spam/abuse)
- DKIM / SPF docs for Migadu setup

## 2026-03-13 16:00 UTC

AgentMail is now publicly accessible at https://cypher.cdnsoft.net/agentmail/

Actions taken:
- Added nginx proxy location /agentmail/ → localhost:3210 in cypher.cdnsoft.net config
- Updated .env: PUBLIC_URL=https://cypher.cdnsoft.net/agentmail, secured ADMIN_KEY
- Restarted agentmail service
- Tested: POST /api/mailboxes returns BTC address, email, IMAP/SMTP config ✓
- BTC address derived: bc1qs84uewv3nu6dmsnp5tku6kef42gq8s5dd5plrk (index 1)

Public API live: https://cypher.cdnsoft.net/agentmail/api/health

Remaining gaps:
- Migadu credentials needed for real IMAP/SMTP (MIGADU_USER, MIGADU_KEY, MIGADU_DOMAIN)
- Without Migadu, mailboxes are provisioned (BTC addresses assigned) but email doesn't actually work
- DNS for agentmail.cdnsoft.net not configured (blocked on Cloudflare token)
- Skill/SKILL.md deferred until production email works end-to-end
