## 2026-03-13 08:00 UTC

Added SMTP send endpoint: POST /api/mailboxes/:id/messages
- New file: src/smtp.js — nodemailer-based send via STARTTLS/Migadu
- Wired into src/api.js as new route, with requireActiveMailbox guard
- Updated index.js endpoint list
- Committed and pushed: 51512dc

AgentMail now has full read+write email loop for agents.
Full API surface:
- Provision mailbox (generates BTC address)
- Check payment / top-up credits
- List messages (IMAP)
- Read message (IMAP)
- Delete message (IMAP)
- Send message (SMTP) — NEW

Next priorities:
- Test end-to-end with real Migadu credentials
- Build simple demo agent that uses the API
- Rate limiting on send endpoint (prevent spam/abuse)
- DKIM / SPF docs for Migadu setup
